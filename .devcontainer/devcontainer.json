{
  "name": "Frappe v15 Dev (Generic)",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",

  // Node 18 + Python 3.11 -> compatibles Frappe v15
  "features": {
    "ghcr.io/devcontainers/features/node:1": { "version": "18" },
    "ghcr.io/devcontainers/features/python:1": { "version": "3.11" }
  },

  "init": true,
  "overrideCommand": false,
  "remoteUser": "vscode",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.debugpy",
        "ms-azuretools.vscode-docker"
      ]
    }
  },

  // Ports stack Frappe
  "forwardPorts": [8000, 3306, 6379],
  "portsAttributes": {
    "8000": { "label": "Frappe Web" },
    "3306": { "label": "MariaDB" },
    "6379": { "label": "Redis" }
  },

  // Variables - génériques (aucune app custom par défaut)
  "containerEnv": {
    "FRAPPE_BRANCH": "version-15",
    "SITE_NAME": "dev.localhost",
    "ADMIN_PASSWORD": "admin",
    "DB_ROOT_USER": "root",
    "DB_ROOT_PASSWORD": "root",

    // Laisse vide => aucune app custom installée.
    "CUSTOM_APP_URL": "",
    "CUSTOM_APP_BRANCH": "develop"
  },

  // Provision complet au 1er build
  "postCreateCommand": "bash -lc 'chmod +x .devcontainer/scripts/init.sh && .devcontainer/scripts/init.sh'",

  // À chaque (re)start : relancer DB/Redis + bench
  "postStartCommand": "bash -lc \"(sudo service mariadb start || sudo service mysql start || true); (sudo service redis-server start || true); (cd ~/frappe-bench && nohup bench start >/tmp/bench.log 2>&1 &)\""
}
